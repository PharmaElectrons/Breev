# Contradiction and Reconciliation Register

Resolution follows the authority order in `PROJECT_CONTEXT.md`. “Resolved” establishes a requirements baseline; it does not mean production implementation exists.

| ID | Topic | Conflict / evidence | Reconciliation | State |
|---|---|---|---|---|
| CON-001 | Product name | Master, brief, code, routes, and package scopes use **Breef**; the latest client note says company/program name is **Breev**. | Stakeholder confirmed Breev for both company and product and approved normalizing new Phase 1 technical identifiers to `breev`/`@breev/*`. Governing sources/prototype remain unchanged evidence. | Resolved |
| CON-002 | Backend stack | Arabic PDF suggests Laravel/PHP + SQLite/cloud PostgreSQL/MySQL; later master/brief require NestJS + PostgreSQL + Drizzle. | Later governing architecture wins: NestJS/PostgreSQL/Drizzle. SQLite/Laravel proposal is superseded. | Resolved |
| CON-003 | Prototype data architecture | `frontend/` uses direct browser Supabase, broad authenticated RLS, and localStorage; target is local API/local PostgreSQL with tenant/service boundaries. | Reuse visual intent only. Do not port direct data access, schema, RLS, auth, or localStorage persistence. | Resolved |
| CON-004 | Prototype maturity | Client called UI ~95% complete visually; repository behavior is mock/unsafe/incomplete. | “95%” applies to appearance/layout/button positions, not domain completeness, security, persistence, or production readiness. | Resolved |
| CON-005 | Product detail side panel | Prototype `AppShell` renders it on every route; PDF used it broadly; latest clarification limits it to Sales and Purchases. | Render it only in Sales and Purchases. Remove it from Inventory, Products, and other screens. | Resolved |
| CON-006 | Shifts vs cash boxes | Conventional shift model appeared in earlier expectations; latest clarification says continuous boxes. | Model continuous Cash Boxes with optional reconciliation snapshots, not forced shifts. | Resolved |
| CON-007 | Clinic UI | Prototype has a large `/clinic` route; master/brief/conversation explicitly exclude clinics. | Quarantine/remove from production navigation and module map. Do not port clinic workflows. | Resolved |
| CON-008 | Messaging channels | Prototype sends WhatsApp, Telegram, and SMS-like actions; conversation confirms WhatsApp only for the first messaging release. | Initial messaging is WhatsApp only. Other channels are deferred/entitled separately. | Resolved |
| CON-009 | Clinical recommendations | Prototype shows heuristic “AI” interactions/substitutes and clinic prescribing; master forbids unlicensed medical advice. | Do not port these behaviors. Only deterministic, licensed/user-entered, traceable alerts may be considered. | Resolved, external-data decision open |
| CON-010 | Purchase row order | Prototype order/Enter flow includes Return, Unit, Profit and skips Selling Price in the required position. | Required keyboard sequence is Item → Qty → Cost → Selling Price → Expiry → next row. Extra fields must not interrupt it. | Resolved |
| CON-011 | Unknown item workflow | Sources require quick creation from unknown barcode/name; prototype silently ignores unknown barcode and has no integrated modal. | Requirement wins; active sale must survive quick product creation. | Resolved gap |
| CON-012 | Quick patient workflow | Sources require modal; prototype uses browser `prompt`. | Keep the workflow, replace prompt with validated modal without losing sale. | Resolved gap |
| CON-013 | Invoice history/edit | PDF discusses editing earlier invoices and prototype deletes/reloads current master data; master requires immutable snapshots and amendments/reversals. | No destructive edits/deletes. Implement draft editing and linked post-publication corrections only; exact amendment policy remains open. | Partly resolved — Q-005 |
| CON-014 | Inventory quantity | Prototype stores/edits quantity and expiry on product and updates it directly during stocktake. | Batches/movements are authoritative; on-hand is derived. Product master does not own a mutable stock total. | Resolved |
| CON-015 | Supplier discount cost | PDF separates price before/after supplier discount; it is unclear which amount values inventory and which posts as discount. | Stakeholder approved IAS 2 treatment: retain gross/discount/net fields, value inventory at net acquisition cost, allocate later purchase-price rebates to inventory/COGS, and separate genuine expense reimbursement or financing. | Resolved; accountant examples pending |
| CON-016 | Cost method | Conversation allows WAC/FIFO/last purchase while master proposes WAC default; IAS 2 identifies FIFO or weighted average for ordinarily interchangeable inventory. | Stakeholder approved pharmacy-level WAC default and accountant-reviewed FIFO alternative. Last-purchase cost is reference only; changes after posting require controlled revaluation. Physical FEFO is independent. | Resolved; accountant examples pending |
| CON-017 | Money model | Scaffold ADR assumes whole IQD integers, but tax/percentage/discount/rounding and non-IQD future behavior are unspecified. | Integer/decimal exact arithmetic is mandatory; storage scale and rounding policy await business/accounting decision. | **Open — Q-004** |
| CON-018 | Invoice number | Scaffold ADR prematurely chose pharmacy/device/year sequence; no client/legal rule confirms prefix, reset, gaps, return/amendment series, or offline terminal ownership. | Global IDs are decided; human numbering remains proposed only. | **Open — Q-005** |
| CON-019 | Sync | Basic one-way and higher two-way are confirmed, while scaffold ADR claims revision vectors/manual queue without ownership rules. | One-way architecture is baseline. Two-way details remain unapproved and must not constrain Phase 1. | Partly resolved — Q-011 |
| CON-020 | Multi-branch | PDF/prototype contain branch transfer and branch tables; master/conversation defer multi-branch. | Keep multi-branch out of core phases; avoid schema/UI assumptions that claim it is implemented. | Resolved |
| CON-021 | Multi-currency | Prototype Accounts supports currencies; product plan defers multi-currency. | Core uses approved single operational currency; multi-currency UI is future-only. | Resolved; base precision Q-004 |
| CON-022 | External integration | Conversation says external-link table UI is free while Zapier/Telegram/API connection is paid; prototype presents broad integration modules. | A local/view table may be free; outbound automation/provider connections are separately entitled and deferred. | Resolved at capability level |
| CON-023 | Future modules in nav | Delivery, e-commerce, marketing, supplier AI comparison, need network, and payroll appear now. | Treat as visual placeholders/future packages, hidden from core navigation until entitled and scheduled. | Resolved |
| CON-024 | Product naming grammar | PDF describes two item-entry modes and ordered generated names; prototype implements only a partial company/name/size/color/form composer. | Stakeholder approved the exact Pharmaceutical and General templates, independent Arabic search name, structured regeneration, immutable snapshots, audited override, identity-independent uniqueness, and versioned configurable templates. | Resolved — ADR-012 |
| CON-025 | Tertiary unit | Requirement is a third clinical unit; prototype sometimes uses “third unit” as reminder-day data. | Tertiary unit is a packaging/clinical quantity unit. Reminder cadence is a separate patient/refill concept. | Resolved |
| CON-026 | Authentication/permissions | Prototype silently creates/signs in a fixed admin and includes renderer passwords/role UI with little enforcement. | Do not reuse. Identity, permissions, secrets, and entitlements are server-authoritative. | Resolved |
| CON-027 | Reports and mutations | Prototype reports can create account transactions/settle debts, while reports should be projections. | Reports are read/export surfaces. Mutations go through named domain commands with separate permissions. | Resolved |
| CON-028 | OCR | Prototype calls a fixed Lovable gateway and allows manual add; target requires provider abstraction, limits, privacy, and reviewed draft. | Preserve the review concept only. No fixed gateway or automatic posting. | Resolved; provider/privacy open |
| CON-029 | Product images/aliases/settings | Prototype stores important data in localStorage; target must work across devices and backups. | Server-owned operational/configuration data is persisted locally/cloud as appropriate; localStorage is only for non-critical per-device UI preference. | Resolved |
| CON-030 | Published prototype | Master references a live Lovable page; it currently returns HTTP 404. | Repository source is the inspected UI baseline; the unreachable page is a documented source limitation. | Resolved limitation |
